apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  namespace: todo
data:
  NODE_ENV: "production"
  # When MYSQL_HOST is set, the backend uses MySQL instead of SQLite.
  MYSQL_HOST: "mysql"
  MYSQL_DB: "todos"
  JWT_EXPIRES_IN: "7d"
  LOG_LEVEL: "info"
